<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About</title>
</head>
<%- include('../partials/head') %>
<%- include('../partials/header') %>
<!-- This start tag <%= is called the “escape output” tag because
if the string in the content has forbidden characters like > and &, the 
characters will be escaped (replaced by HTML codes) in the output string.
-->
<body>
    <ul>
        <% about.forEach((about)=> { %>
            <li>
                <h2>
                    <%= about.title %>
                </h2>
                <p>
                    <%= about.body %>
                </p>
            </li>
            <hr />
        <% }) %>
    </ul>

</body>
<%- include('../partials/footer') %>

</html>